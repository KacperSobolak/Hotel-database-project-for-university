<UserControl x:Class="Hotel.MVVM.View.ReservationsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Hotel.MVVM.View"
             mc:Ignorable="d" 
             Background="LightGray"
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <ListView Grid.Row="0" ItemsSource="{Binding Reservations}" Margin="10">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="Room" DisplayMemberBinding="{Binding Room.RoomNumber}" Width="100" />
                    <GridViewColumn Header="Guest" DisplayMemberBinding="{Binding Guest.Name}" Width="150" />
                    <GridViewColumn Header="Start Date" DisplayMemberBinding="{Binding StartDate}" Width="150" />
                    <GridViewColumn Header="End Date" DisplayMemberBinding="{Binding EndDate}" Width="150" />
                    <GridViewColumn Header="Total Price" DisplayMemberBinding="{Binding TotalPrice}" Width="100" />
                    <GridViewColumn Header="Akcje" Width="100">
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <Button Content="Edytuj" Command="{Binding DataContext.EditReservationCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                        CommandParameter="{Binding}" />
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>
                </GridView>
            </ListView.View>
        </ListView>

        <Button Grid.Row="1" Content="Add Reservation" Command="{Binding AddReservationCommand}" Height="40" FontSize="16" Width="200" Margin="10" HorizontalAlignment="Center" />
    </Grid>
</UserControl>
